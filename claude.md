# 매장 CCTV 분석 시뮬레이션 대시보드 프롬프트

## 프로젝트 컨텍스트

이것은 **"매장 CCTV 영상에서의 실시간 고객 속성 분류 및 체류 히트맵 기반 상품 배치 최적화"** 캡스톤 프로젝트의 프론트엔드 대시보드이다. 백엔드에서 CCTV 영상을 분석하여 고객을 4클래스(성년 남성, 성년 여성, 미성년 남성, 미성년 여성)로 분류하고 동선을 추적한 데이터를 시각화한다.

---

## 프롬프트

React와 HTML5 Canvas를 결합한 **실시간 소매 매장 동선 시뮬레이션 및 분석 대시보드**를 개발해줘.

### 기술 스택
React (Functional Components), Hooks (`useRef`, `useEffect`, `useState`, `useCallback`), HTML5 Canvas API, Lucide-react (아이콘), Recharts (차트).

---

### 1. 하이브리드 아키텍처

**React 영역:**
- 좌측 사이드바: 시뮬레이션 제어판, 매대 ROI 설정
- 우측 패널: 실시간 통계, 차트, CSV 다운로드
- 하단: 히트맵 이미지 저장, 구매 내역 CSV 다운로드 버튼

**Canvas 영역:**
- `StoreCanvas` 컴포넌트에서 `useRef`로 캔버스 컨텍스트 접근
- `requestAnimationFrame`으로 60fps 에이전트 이동 렌더링
- 매대(진열대) 장애물, ROI 영역, 히트맵 오버레이를 레이어로 관리

---

### 2. 데이터 모델

**고객 에이전트 4클래스:**
```javascript
const CUSTOMER_CLASSES = {
  adult_male:   { label: "성년 남성",   color: "#FF9600", icon: "👨" },
  adult_female: { label: "성년 여성",   color: "#9314FF", icon: "👩" },
  minor_male:   { label: "미성년 남성", color: "#00C800", icon: "👦" },
  minor_female: { label: "미성년 여성", color: "#00A5FF", icon: "👧" },
};
```

**에이전트 객체 구조:**
```javascript
{
  id: number,
  classType: "adult_male" | "adult_female" | "minor_male" | "minor_female",
  x: number, y: number,
  targetX: number, targetY: number,
  velocity: { x: number, y: number },
  state: "entering" | "browsing" | "shopping" | "checkout" | "leaving",
  visitedZones: string[],        // 방문한 매대 ROI 목록
  dwellTime: { [zone: string]: number },  // 매대별 체류 시간(초)
  purchaseHistory: { zone: string, item: string, price: number }[],  // 구매 내역
  entryTime: number,
  height_cm: number,             // 추정 키 (시뮬레이션용)
}
```

**매대(진열대) ROI 구조:**
```javascript
{
  id: string,           // "zone_A", "zone_B", ...
  label: string,        // "과자 매대", "음료 매대", ...
  x: number, y: number, width: number, height: number,
  category: string,     // 상품 카테고리
  items: { name: string, price: number, targetClass: string }[],
}
```

---

### 3. 매장 레이아웃

Canvas 위에 다음 매장 요소를 배치:
- **입구** (하단 중앙): 고객이 유입되는 지점
- **계산대** (하단 좌측): 2~3개 계산대
- **진열대 6~8개**: 직사각형 장애물로 표현, 각각 ROI 영역 설정
  - 과자/스낵 매대, 음료 매대, 생활용품 매대, 화장품 매대, 문구 매대, 냉동식품 매대
- **통로**: 진열대 사이 이동 가능 영역

매대별로 **타겟 고객층**을 설정:
- 과자/스낵: 미성년 선호
- 화장품: 성년 여성 선호
- 음료: 전 연령 균등
- 문구: 미성년 선호
- 생활용품: 성년 선호

---

### 4. 에이전트 행동 알고리즘

**A* Pathfinding:**
- 매장 진열대(장애물)를 피해 목적지까지 최단 경로 계산
- 그리드 기반 (10px 단위)

**Steering Behaviors:**
- `Separation`: 에이전트 간 충돌 방지 (최소 거리 15px)
- `Arrive`: 목적지 근처 감속
- `Wander`: 쇼핑 중 매대 주변 배회

**클래스별 행동 패턴:**
- 각 클래스는 선호 매대에 갈 확률이 높음 (70% 선호 매대, 30% 랜덤)
- 성년은 평균 체류 시간이 길고 (3~8초/매대), 미성년은 짧음 (1~4초/매대)
- 구매 확률: 선호 매대 방문 시 60%, 비선호 매대 방문 시 20%
- 미성년은 평균 이동 속도가 빠름

---

### 5. 핵심 기능

#### 5-1. 실시간 히트맵
- 2차원 배열(`heatmapGrid[y][x]`)로 고객 위치 카운트 누적
- **4클래스별 개별 히트맵** + **통합 히트맵** 전환 가능
- Canvas 위에 반투명 색상 레이어로 오버레이 (jet 컬러맵: 파랑→초록→노랑→빨강)
- **히트맵 이미지 저장 버튼**: Canvas를 `toDataURL('image/png')`로 변환하여 다운로드
  - 개별 클래스 히트맵 4장 + 통합 히트맵 1장 + 비교(2×2) 히트맵 1장
  - 파일명: `heatmap_adult_male.png`, `heatmap_combined.png`, `heatmap_comparison.png`

#### 5-2. 구매 내역 CSV 생성 및 다운로드
- 시뮬레이션 중 각 에이전트의 구매 이력을 실시간 기록
- **CSV 다운로드 버튼** 클릭 시 아래 포맷으로 저장:

```csv
고객ID,고객분류,고객분류명,방문매대,상품명,가격,체류시간_초,구매시각
1,adult_male,성년 남성,음료 매대,콜라,1500,5.2,00:01:23
1,adult_male,성년 남성,과자 매대,새우깡,1800,3.1,00:02:45
2,minor_female,미성년 여성,문구 매대,색연필,3000,2.8,00:01:50
```

- **클래스별 구매 통계 요약 CSV도 별도 생성:**

```csv
분류,분류명,총구매건수,총구매금액,평균체류시간_초,가장많이방문한매대
adult_male,성년 남성,45,125000,5.3,음료 매대
adult_female,성년 여성,38,180000,6.1,화장품 매대
minor_male,미성년 남성,22,44000,2.1,과자 매대
minor_female,미성년 여성,28,56000,2.5,문구 매대
```

#### 5-3. 동적 매대 관리
- 사용자가 React UI에서 매대 위치/크기를 드래그로 조정 가능
- '통로 폐쇄' 설정 시 에이전트가 즉시 경로 재계산
- 매대 상품 변경 시 에이전트 선호도 반영

#### 5-4. 실시간 대시보드 연동
- 시뮬레이션 내부 상태를 React State와 동기화:
  - 현재 매장 내 인원 수 (클래스별)
  - 매대별 혼잡도 (체류 인원 / 면적)
  - 계산대 대기 인원
  - 시간당 예상 매출 (클래스별)
  - 매대별 체류 시간 랭킹

---

### 6. UI 구성 요소

#### 좌측 사이드바 (280px)
```
┌─────────────────────────┐
│ 🏪 매장 시뮬레이션      │
├─────────────────────────┤
│ ▶ 시작 / ⏸ 일시정지 / 🔄 리셋 │
│ 속도: [━━━━━●━━] 2x     │
├─────────────────────────┤
│ 고객 유입 설정           │
│  유입 속도: [━━━●━━] 3/분│
│  ┌ 성년 남성  [━●━] 25% │
│  ├ 성년 여성  [━●━] 25% │
│  ├ 미성년 남성 [━●━] 25% │
│  └ 미성년 여성 [━●━] 25% │
├─────────────────────────┤
│ 표시 옵션                │
│  ☑ 히트맵 오버레이       │
│  ☑ 경로 표시            │
│  ☑ 매대 ROI 표시        │
│  히트맵 모드: [통합 ▼]   │
│   - 통합 / 성년남 / 성년여│
│   - 미성년남 / 미성년여   │
├─────────────────────────┤
│ 이벤트                   │
│  [🏷 세일 시작]          │
│  [🚧 통로 폐쇄]         │
│  [📢 안내 방송]          │
└─────────────────────────┘
```

#### 중앙 Canvas (800×600)
- 매장 평면도 + 에이전트 + 히트맵 오버레이
- 에이전트: 클래스 색상의 원(반지름 5px) + 이동 방향 화살표
- 매대: 회색 직사각형 + 라벨
- ROI: 점선 테두리
- 히트맵: 반투명 jet 컬러맵 오버레이

#### 우측 패널 (320px)
```
┌──────────────────────────┐
│ 📊 실시간 분석            │
├──────────────────────────┤
│ 현재 매장 인원: 23명      │
│  👨 성년 남성: 7          │
│  👩 성년 여성: 6          │
│  👦 미성년 남성: 5        │
│  👧 미성년 여성: 5        │
├──────────────────────────┤
│ 🔥 혼잡 매대 Top 3       │
│  1. 과자 매대 (8명)       │
│  2. 음료 매대 (5명)       │
│  3. 화장품 매대 (4명)     │
├──────────────────────────┤
│ 💰 예상 매출              │
│  [Recharts 막대 그래프]   │
│  성년남: ₩125,000        │
│  성년여: ₩180,000        │
│  미성년남: ₩44,000       │
│  미성년여: ₩56,000       │
├──────────────────────────┤
│ 📈 매대별 체류 시간       │
│  [Recharts 가로 막대]     │
├──────────────────────────┤
│ 📥 데이터 다운로드        │
│  [히트맵 이미지 저장]     │
│  [구매 내역 CSV]         │
│  [구매 통계 CSV]         │
│  [체류 분석 CSV]         │
└──────────────────────────┘
```

---

### 7. 데이터 다운로드 기능 상세

#### 히트맵 이미지 저장
```javascript
// 히트맵 저장 함수
const saveHeatmapImage = (classType) => {
  // 1. 오프스크린 캔버스에 매장 배경 + 히트맵만 그리기 (에이전트 제외)
  // 2. 클래스별 개별 히트맵 or 통합 히트맵
  // 3. canvas.toDataURL('image/png')로 변환
  // 4. <a> 태그로 다운로드 트리거
  // 파일명: heatmap_{classType}_{timestamp}.png
};

// 2×2 비교 히트맵 (4클래스를 한 이미지에)
const saveComparisonHeatmap = () => {
  // 2×2 그리드 캔버스 생성 (1600×1200)
  // 각 칸에 클래스별 히트맵 + 라벨
};
```

#### CSV 생성 및 다운로드
```javascript
// 구매 내역 CSV
const downloadPurchaseCSV = () => {
  const header = "고객ID,고객분류,고객분류명,방문매대,상품명,가격,체류시간_초,구매시각\n";
  const rows = allPurchases.map(p => 
    `${p.agentId},${p.classType},${CUSTOMER_CLASSES[p.classType].label},${p.zone},${p.item},${p.price},${p.dwellTime},${p.time}`
  ).join("\n");
  // BOM 추가 (Excel 한글 호환): "\uFEFF" + header + rows
  downloadFile(`purchase_history_${timestamp}.csv`, "\uFEFF" + header + rows);
};

// 클래스별 구매 통계 요약 CSV
const downloadSummaryCSV = () => {
  // 클래스별 집계: 총 구매건수, 총 금액, 평균 체류시간, 최다 방문 매대
};

// 매대별 체류 분석 CSV  
const downloadDwellCSV = () => {
  // 매대 × 클래스 매트릭스: 각 매대에서 각 클래스의 평균 체류 시간
};
```

---

### 8. 상품 배치 인사이트

시뮬레이션 결과를 기반으로 **자동 인사이트 생성:**

```
💡 추천 인사이트:
• "과자 매대에 미성년 남성이 34% 집중 → 미성년 타겟 상품 배치 적합"
• "화장품 매대 성년 여성 체류 시간 6.1초 (최장) → 프리미엄 상품 진열 추천"  
• "음료 매대와 계산대 사이 동선에 충동구매 상품 배치 권장"
• "생활용품 매대 방문율 12%로 최저 → 위치 변경 또는 프로모션 필요"
```

이 인사이트는 우측 패널 하단에 카드 형태로 표시하고, 히트맵 데이터 + 구매 데이터를 종합하여 자동 생성.

---

### 9. 성능 최적화

- 히트맵 그리드는 매장 크기의 1/4 해상도 (200×150)로 유지
- 에이전트 100명 이상 시 경로 계산 주기를 조절 (매 프레임 → 5프레임마다)
- Canvas 렌더링은 더티 영역(dirty region)만 다시 그리기
- CSV 데이터는 메모리에 누적, 다운로드 시에만 문자열 변환

---

### 10. 반응형 디자인

- 최소 너비: 1200px
- Canvas 크기: 뷰포트에 맞게 자동 조절 (비율 유지)
- 사이드바: 모바일에서 토글 가능
- 색상 테마: 다크 모드 기본

---

이 전체를 **단일 React 컴포넌트 파일(.jsx)**로 구현해줘. 외부 라이브러리는 Recharts와 Lucide-react만 사용.

import React from 'react'
import ReactDOM from 'react-dom/client'
import RetailDashboard from '../Dashboard.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RetailDashboard />
  </React.StrictMode>,
)
